# 数组和字符串方法

数组方法：

增删查改：push，pop，unshift,shift,splice,concat（不改变）,slice（不改变）

遍历方法：forEach，map，filter，find（返回第一个匹配的元素），findIndex,some,every,reduce

排序反转：sort，reverse

转换与拼接（都不改变）：join，toString，Array.from，flat

ES6新增：includes(不改变)

字符串方法：

includes()，indexOf()，slice()，split(按分隔符拆分为数组)，

