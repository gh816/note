# 突击一下

伪类和伪元素的区别？

伪类是选择元素的特定状态（如鼠标悬停），伪元素是选择元素的特定部分（如首字母，首字母等）

flex：1；代表flex-grow:1;flex-shrink: 1;flex-basis"0;

flex-grow：定义项目在容器有剩余空间时的放大比例

flex-shrink: 定义项目在空间不足时的收缩比例

flex-basis: 定义项目在分配多余空间之前的初始大小